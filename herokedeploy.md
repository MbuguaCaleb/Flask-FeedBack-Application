```

To deploy to Heroku you must have the Heroku CLI Installed...

```

```
The steps are
(i)heroku login

(ii)heroku create  heroku create lexusfeedbackappbycaleb

(iii)create database in heroku

heroku addons:create heroku-postgresql:hobby-dev --app lexusfeedbackappbycaleb

refers To->
heroku addons:create heroku-postgresql:hobby-dev --app appname

heroku-postgresql:hobby-dev --lexusfeedbackbycaleb
(iv)Getting APP URI


heroku config --app lexusfeedbackappbycaleb

refers To->
heroku config --app appname

```
